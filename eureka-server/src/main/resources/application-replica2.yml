server:
  port: 8003

spring:
  application:
    name: eureka-serve

eureka:
  instance:
    hostname: replica2
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://replica2:8001/eureka/

